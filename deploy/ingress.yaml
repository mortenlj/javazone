---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: javazone
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  rules:
    - host: javazone.home.ibidem.no
      http:
        paths:
          - path: /
            backend:
              service:
                name: javazone
                port:
                  name: http
  tls:
    - hosts:
      - javazone.home.ibidem.no
      secretName: javazone-ingress-cert

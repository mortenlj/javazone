{% extends 'base.html.j2' %}

{% block content %}
    <p>Welcome to {{ request.app.title }}</p>
    <p>
        This is a service to help you plan your time at the <a href="https://javazone.no">JavaZone conference</a>.
        This service is not affiliated with JavaZone, nor does it have any official endorsement from the organizers.
    </p>
    <p>The service is considered in a closed beta phase, available by invite only.</p>
    <p>Below are all sessions in this year's JavaZone conference.</p>

    <div class="container text-center">
        <ul class="nav nav-pills nav-fill" id="sessionsTab" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="tuesday-tab" data-bs-toggle="pill" data-bs-target="#tuesday" type="button" role="tab" aria-controls="tuesday" aria-selected="true">Tuesday</button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="wednesday-tab" data-bs-toggle="pill" data-bs-target="#wednesday" type="button" role="tab" aria-controls="wednesday" aria-selected="false">Wednesday</button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="thursday-tab" data-bs-toggle="pill" data-bs-target="#thursday" type="button" role="tab" aria-controls="thursday" aria-selected="false">Thursday</button>
            </li>
        </ul>
        <div class="tab-content" id="sessionsTabContent">
            <div class="tab-pane fade show active" id="tuesday" role="tabpanel">
                {% for session in sessions["tuesday"] %}
                    {% include "widgets/session_small.html.j2" %}
                {% endfor %}
            </div>
            <div class="tab-pane fade" id="wednesday" role="tabpanel">
                {% for session in sessions["wednesday"] %}
                    {% include "widgets/session_small.html.j2" %}
                {% endfor %}
            </div>
            <div class="tab-pane fade" id="thursday" role="tabpanel">
                {% for session in sessions["thursday"] %}
                    {% include "widgets/session_small.html.j2" %}
                {% endfor %}
            </div>

        </div>
    </div>
{% endblock %}

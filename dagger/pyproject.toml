[project]
name = "main"
version = "0.0.0"
dependencies = [
    "dagger-io",
    "jinja2",
    "toml>=0.10.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
dagger-io = { path = "sdk", editable = true }
